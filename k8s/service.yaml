apiVersion: v1
kind: Service
metadata:
  name: todo-app-service
  namespace: default   # 🔥 ADD THIS
spec:
  type: LoadBalancer
  selector:
    app: todo-app
  ports:
    - port: 80
      targetPort: 3000
